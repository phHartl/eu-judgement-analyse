swagger: "2.0"
info:
  description: This is the swagger file that goes with our server code
  version: "1.0.0"
  title: Swagger REST Article
consumes:
  - "application/json"
produces:
  - "application/json"

basePath: "/api"

# Paths supported by the server application
paths:
  /mongo:
    get:
      operationId: "mongo.get_docs_by_date"
      tags:
        - "Judgements"
      summary: "The test data structure supported by the server application"
      description: "Get the list of judgements by date"
      responses:
        200:
          description: "Successful operation"
          schema:
            type: "array"
            items:
              properties:
                name:
                  type: "string"
                year:
                  type: "string"
                content:
                  type: "object"